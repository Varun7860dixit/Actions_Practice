name: multiple steps

on:
  push:

jobs:
  demo:

    runs-on: uubuntu-latest 

    steps: 
      - name: step1
        run: echo "starting"

      - name: step2
        run: pwd

      - name: step3
        run: ls

      - name: step4
        run: echo "completed"
